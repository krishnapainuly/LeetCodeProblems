package com.problems.templates;

public class BreadthFirstStrategy {

	// Initialize and empty result array

	// Handle an empty tree as a special edge case(return empty result array)

	// Create an empty queue and push the root of the tree into it

	// while queue is not empty
		// count number of nodes in the queue
		// Repeat that many times
		// Pop the next node from the front of the Queue
		// Append the value of the result array
	
		// If it has a left child, add the left child to the queue
		// If it has a right child, add the right child to the queue
}
